# Copy to supabase/.env (gitignored). Required for local Google OAuth.
# Get values from Google Cloud → APIs & Services → Credentials → OAuth 2.0 Client ID.
#
# After editing:  supabase stop && supabase start

SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET=
